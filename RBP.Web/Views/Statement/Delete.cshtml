@using RBP.Web.Utils
@model StatementViewModel

<div class="w-100 h-100 p-2" style="display: flex; flex-direction: column;">
    <header>
        <h3>@Model.PageTitle</h3>
        @if (!string.IsNullOrEmpty(Model.ErrorMessage))
        {
            @Html.ErrorMessage(Model.ErrorMessage)
        }
        @if (!string.IsNullOrEmpty(Model.OkMessage))
        {
            @Html.OkMessage(Model.OkMessage)
        }
    </header>
    <main style="flex: 1; overflow-y: visible; overflow-x: hidden;">
        @if (string.IsNullOrEmpty(Model.ErrorMessage) && string.IsNullOrEmpty(Model.OkMessage))
        {
            <div class='card error-message border-danger mt-0 mb-1'>
                <div class='card-body text-danger px-3 py-1'>
                    <p class='card-text'>
                        Вы действительно хотите удалить эту ведомость?
                    </p>
                </div>
            </div>
        }
    </main>
    <footer>
        @if (string.IsNullOrEmpty(Model.ErrorMessage) && string.IsNullOrEmpty(Model.OkMessage))
        {
            <a class="btn btn-primary btn-block" href="/Statement/Edit?Id=@Model.Statement.Id">Нет, отмена</a>
            <a class="btn btn-danger btn-block" href="/Statement/DeleteFinnaly?Id=@Model.Statement.Id">Да, удалить</a>
        }
        else
        {
            <a class="btn btn-primary btn-block" href="/Statement/List?SegmentId=@Model.Statement.Segment.Id">К ведомостям</a>
        }
    </footer>
</div>

